let user score = 0;
let computerScore = 0;

const choices =document.querySelector All("choices")

choices.array.forEach(choice => {
    choice.addEventlistener("click"()=>{
    console.log("chois=ce was clicked")
});
});